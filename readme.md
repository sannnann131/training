「いきなりはじめるPHP」を元に作成したアンケートアプリです。
本の内容をほぼそのまま記載していますが、アンケート回答後のメール自動送信はそのままだとエラーになるので省いています。

名前、メールアドレス、意見を入力し確認ボタンを押すと確認ページへ移行します。
確認ページでは入力内容が表示され、送信ボタンを押すと入力内容が再度表示され送信された旨が表示されます。

管理者ページとして、本の内容そのままなので誰でもアクセスできる状態(ログイン機能はない)ではありますが送信された内容を全て閲覧することは可能になっています。(ローカル環境のみ)